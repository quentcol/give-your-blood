<%= simple_form_for(day) do |form| %>
  <% if day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(day.errors.count, "error") %> prohibited this day from being saved:</h2>
      <ul>
        <% day.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.input :name, label: "Day Name", input_html: { class: "form-control" } %>

  <div class="actions">
    <%= form.button :submit, "Save", class: "btn btn-primary" %>
  </div>
<% end %>