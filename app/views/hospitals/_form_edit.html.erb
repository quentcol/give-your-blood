<div class="container" style="padding: 50px; margin-top: 60px;">
  <%= simple_form_for(@hospital, url: hospital_path(@hospital), method: "patch") do |form| %>
    <% if @hospital.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@hospital.errors.count, "error") %> prohibited this hospital from being saved:</h2>
        <ul>
        <% @hospital.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <%= form.input :hospital_name, label: "Hospital Name", input_html: { class: "form-control" } %>

    <div class="actions">
      <%= form.button :submit, "Save", class: "btn btn-primary" %>
    </div>
  <% end %>
</div>